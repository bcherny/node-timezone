test:
  override:
    - nvm install v0.12 && nvm use v0.12 && npm test
    - nvm install v4.0 && nvm use v4.0 && npm test
    - nvm install v6.0 && nvm use v6.0 && npm test
    - nvm install v7.0 && nvm use v7.0 && npm test
